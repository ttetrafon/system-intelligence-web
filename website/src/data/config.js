// URLs
export const domainRoot = "https://system-intelligence.com";
export const gameServiceUrl = "http://localhost:3000"; // TODO: make this work from env, so that it uses localhost in dev mode but the service url when built

// routes
export const routeTableOfContents = "/data/web-app-menus";
